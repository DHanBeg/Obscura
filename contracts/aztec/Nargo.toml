[package]
name = "obs_token_v2"
type = "contract"
authors = ["Obscura Blockchain Team"]
compiler_version = ">=0.30.0"

# Aztec.nr standart kütüphane bağımlılığı.
# tag'i kullandığın sandbox sürümüne göre sabitle (aztec-up ile gelen sürüm).
# Örn. sandbox v0.47.x için: tag = "aztec-packages-v0.47.0"
[dependencies]
aztec = { git = "https://github.com/AztecProtocol/aztec-packages", tag = "aztec-packages-v0.47.0", directory = "noir-projects/aztec-nr/aztec" }
